<%= simple_form_for(project) do |f| %>
  <%= f.input :name %><br />
  <%= f.input :description %><br />

  <%= f.submit "#{project.new_record? ? "Create" : "Update"} Project", class: "btn-primary" %>
<% end %>
